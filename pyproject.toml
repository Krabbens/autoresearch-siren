[project]
name = "autoresearch-siren"
version = "0.1.0"
description = "Autonomous research harness for SIREN V8 VQ-VAE (fork of karpathy/autoresearch)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "siren-codec",
    "pesq>=0.0.4",
    "pystoi>=0.4.1",
]

[tool.uv.sources]
siren-codec = { path = "../SIREN", editable = true }
